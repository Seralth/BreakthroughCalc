name: breakthrough_calc
description: Breakthrough Calculator — Android/mobile port (Flutter).
publish_to: none
version: 2.18.1+29

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  archive: ^4.0.9
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/data/breakthrough.json
    - assets/data/sources.json
